# Generated by `npm run db:types` (supabase gen types). Formatting it means the
# file flips back and forth between the generator's output and Prettier's on
# every regeneration, burying real schema changes in reformatting noise.
src/lib/supabase/database.types.ts

# Build and dependency output
.next/
node_modules/
package-lock.json
